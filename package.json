{
  "name": "@suiji/root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm run -C webapp build",
    "dev": "pnpm run -C webapp dev",
    "lint": "eslint \"**/*.{vue,ts,tsx,js,jsx}\"",
    "lint:lint-staged": "lint-staged",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.14.2",
    "@commitlint/cli": "^16.0.2",
    "@commitlint/config-conventional": "^16.0.0",
    "eslint": "^8.6.0",
    "husky": "^7.0.4",
    "lint-staged": "^12.1.7",
    "pnpm": "^6.25.1",
    "typescript": "^4.5.4",
    "vue-tsc": "^0.30.2"
  },
  "lint-staged": {
    "*.{vue,ts,tsx,js,jsx}": [
      "eslint --fix"
    ]
  }
}
